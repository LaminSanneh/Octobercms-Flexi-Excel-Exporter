# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true
        hidden: true
    title:
        label: Title
        type: text
    columns:
        label: Columns
        type: checkboxlist
        comment: Select the list of columns to export in the excel exported file
    model:
        type: text
        label: Selected Model

